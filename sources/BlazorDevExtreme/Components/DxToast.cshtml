@implements IDisposable
@inherits DxBlazorCompoment<DevExpress.Ui.DxToastOptions>

<div id="@Id"></div>

@functions {
	public DxToast() : base("DxToast") { }
	protected override DevExpress.Ui.DxToastOptions CreateOptions() {
		return new DevExpress.Ui.DxToastOptions {
			closeOnBackButton = closeOnBackButton,
			closeOnClick = closeOnClick,
			closeOnOutsideClick = closeOnOutsideClick,
			closeOnSwipe = closeOnSwipe,
			displayTime = displayTime,
			height = height,
			maxWidth = maxWidth,
			message = message,
			minWidth = minWidth,
			shading = shading,
			type = type,
			width = width,
		};
	}
	protected override void Init(DevExpress.Ui.DxToastOptions options) {
		DxToastInterop.Init(Id, options);
	}
	bool? closeOnBackButton;
	public bool? CloseOnBackButton {
		get => isInitialized ? DxToastInterop.GetCloseOnBackButton(Id) : closeOnBackButton;
		set {
			if (isInitialized) {
				DxToastInterop.SetCloseOnBackButton(Id, value);
			} else {
				this.closeOnBackButton = value;
			}
		}
	}
	bool? closeOnClick;
	public bool? CloseOnClick {
		get => isInitialized ? DxToastInterop.GetCloseOnClick(Id) : closeOnClick;
		set {
			if (isInitialized) {
				DxToastInterop.SetCloseOnClick(Id, value);
			} else {
				this.closeOnClick = value;
			}
		}
	}
	object closeOnOutsideClick;
	public object CloseOnOutsideClick {
		get => isInitialized ? DxToastInterop.GetCloseOnOutsideClick(Id) : closeOnOutsideClick;
		set {
			if (isInitialized) {
				DxToastInterop.SetCloseOnOutsideClick(Id, value);
			} else {
				this.closeOnOutsideClick = value;
			}
		}
	}
	bool? closeOnSwipe;
	public bool? CloseOnSwipe {
		get => isInitialized ? DxToastInterop.GetCloseOnSwipe(Id) : closeOnSwipe;
		set {
			if (isInitialized) {
				DxToastInterop.SetCloseOnSwipe(Id, value);
			} else {
				this.closeOnSwipe = value;
			}
		}
	}
	float? displayTime;
	public float? DisplayTime {
		get => isInitialized ? DxToastInterop.GetDisplayTime(Id) : displayTime;
		set {
			if (isInitialized) {
				DxToastInterop.SetDisplayTime(Id, value);
			} else {
				this.displayTime = value;
			}
		}
	}
	object height;
	public object Height {
		get => isInitialized ? DxToastInterop.GetHeight(Id) : height;
		set {
			if (isInitialized) {
				DxToastInterop.SetHeight(Id, value);
			} else {
				this.height = value;
			}
		}
	}
	object maxWidth;
	public object MaxWidth {
		get => isInitialized ? DxToastInterop.GetMaxWidth(Id) : maxWidth;
		set {
			if (isInitialized) {
				DxToastInterop.SetMaxWidth(Id, value);
			} else {
				this.maxWidth = value;
			}
		}
	}
	string message;
	public string Message {
		get => isInitialized ? DxToastInterop.GetMessage(Id) : message;
		set {
			if (isInitialized) {
				DxToastInterop.SetMessage(Id, value);
			} else {
				this.message = value;
			}
		}
	}
	object minWidth;
	public object MinWidth {
		get => isInitialized ? DxToastInterop.GetMinWidth(Id) : minWidth;
		set {
			if (isInitialized) {
				DxToastInterop.SetMinWidth(Id, value);
			} else {
				this.minWidth = value;
			}
		}
	}
	bool? shading;
	public bool? Shading {
		get => isInitialized ? DxToastInterop.GetShading(Id) : shading;
		set {
			if (isInitialized) {
				DxToastInterop.SetShading(Id, value);
			} else {
				this.shading = value;
			}
		}
	}
	string type;
	public string Type {
		get => isInitialized ? DxToastInterop.GetType(Id) : type;
		set {
			if (isInitialized) {
				DxToastInterop.SetType(Id, value);
			} else {
				this.type = value;
			}
		}
	}
	object width;
	public object Width {
		get => isInitialized ? DxToastInterop.GetWidth(Id) : width;
		set {
			if (isInitialized) {
				DxToastInterop.SetWidth(Id, value);
			} else {
				this.width = value;
			}
		}
	}
	protected override async Task OnInitAsync() {
		await base.OnInitAsync();
	}
	public void Dispose() {
	}
}
