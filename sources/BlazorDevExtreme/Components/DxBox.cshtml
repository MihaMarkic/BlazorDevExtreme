@implements IDisposable
@inherits DxBlazorCompoment<DevExpress.Ui.DxBoxOptions>

<div id="@Id"></div>

@functions {
	public DxBox() : base("DxBox") { }
	protected override DevExpress.Ui.DxBoxOptions CreateOptions() {
		return new DevExpress.Ui.DxBoxOptions {
			align = align,
			crossAlign = crossAlign,
			direction = direction,
		};
	}
	protected override void Init(DevExpress.Ui.DxBoxOptions options) {
		DevExpress.Ui.DxBoxInterop.Init(Id, options);
	}
	string align;
	public string Align {
		get => isInitialized ? DevExpress.Ui.DxBoxInterop.GetAlign(Id) : align;
		set {
			if (isInitialized) {
				DevExpress.Ui.DxBoxInterop.SetAlign(Id, value);
			} else {
				this.align = value;
			}
		}
	}
	string crossAlign;
	public string CrossAlign {
		get => isInitialized ? DevExpress.Ui.DxBoxInterop.GetCrossAlign(Id) : crossAlign;
		set {
			if (isInitialized) {
				DevExpress.Ui.DxBoxInterop.SetCrossAlign(Id, value);
			} else {
				this.crossAlign = value;
			}
		}
	}
	string direction;
	public string Direction {
		get => isInitialized ? DevExpress.Ui.DxBoxInterop.GetDirection(Id) : direction;
		set {
			if (isInitialized) {
				DevExpress.Ui.DxBoxInterop.SetDirection(Id, value);
			} else {
				this.direction = value;
			}
		}
	}
	protected override async Task OnInitAsync() {
		await base.OnInitAsync();
	}
	public void Dispose() {
	}
}
